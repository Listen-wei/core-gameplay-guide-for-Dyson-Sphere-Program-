第1周：理解问题 + 数据准备阶段

目标：

理解任务定义与数据结构

获取并清洗数据

掌握SVM的基础使用

任务内容：

下载数据集：
👉 Online Shoppers Intention Dataset (UCI)

数据说明：

约12,000条记录

每条记录包含浏览页数、访问时长、周末/非周末、广告点击等特征

标签：Revenue（是否购买：True/False）

使用Python读取并初步探索：

import pandas as pd

data = pd.read_csv("online_shoppers_intention.csv")
print(data.head())
print(data.info())
print(data['Revenue'].value_counts())


进行数据预处理：

填补缺失值（若有）

对类别变量进行 one-hot 编码

对数值变量标准化（StandardScaler）

练习简单的SVM分类器：

from sklearn.svm import SVC
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler
from sklearn.metrics import accuracy_score

# X, y 分割
X = data.drop(columns=['Revenue'])
y = data['Revenue']

# 训练集划分
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.3, random_state=42)

# 训练
clf = SVC(kernel='rbf', C=1.0)
clf.fit(X_train, y_train)
print("Accuracy:", clf.score(X_test, y_test))

🗓 第2周：模型实验阶段

目标：

对比不同核函数和参数

初步分析影响模型效果的因素

任务内容：

尝试不同核函数：

kernel='linear'

kernel='poly'

kernel='rbf'

kernel='sigmoid'

对比不同超参数：

改变 C（惩罚参数） 和 gamma（RBF核参数）

使用 GridSearchCV 自动搜索最佳参数

记录结果表格：

Kernel	C	gamma	Accuracy	支持向量数
linear	1	-	0.86	1210
rbf	10	0.1	0.91	980
...	...	...	...	...

可视化分类结果（可用PCA降维到2D）

from sklearn.decomposition import PCA
import matplotlib.pyplot as plt

X_pca = PCA(n_components=2).fit_transform(X_test)
plt.scatter(X_pca[:,0], X_pca[:,1], c=clf.predict(X_test))
plt.title("SVM decision (PCA 2D)")
plt.show()

🗓 第3周：模型改进与分析阶段

目标：

提升模型表现

分析哪些特征最关键（可解释性）

任务内容：

尝试以下改进：

进行特征选择（如SelectKBest、RFE）

对类别不平衡进行加权：class_weight='balanced'

尝试分层采样（StratifiedKFold）

分析重要特征：

对线性核SVM，查看coef_了解权重；

或结合树模型（如RandomForest）对比特征重要性。

写出分析结论：

哪些特征最影响购买意愿？（如“产品页面数”“周末访问”等）

哪种核函数表现最好？

参数变化对结果的影响？

🗓 第4周：结果总结 + 可视化展示阶段

目标：

形成完整的实验报告或展示文件

任务内容：

输出完整的性能指标：

准确率 (Accuracy)

精确率 / 召回率 / F1-score

混淆矩阵

from sklearn.metrics import classification_report, confusion_matrix
print(confusion_matrix(y_test, clf.predict(X_test)))
print(classification_report(y_test, clf.predict(X_test)))


绘制：

不同核函数的准确率柱状图

混淆矩阵热图

决策边界（PCA降维可视化）

撰写实验报告（或幻灯片）：
推荐报告结构：

背景与问题定义（消费者购买预测）

数据描述

实验设计（SVM模型、参数选择）

实验结果与分析

结论与未来工作（如尝试集成学习或深度特征）

🧠 可选的进阶方向（未来可深入）

📊 特征工程方向：尝试设计新特征（例如“平均停留时间 × 页面数”）

⚙️ 模型融合方向：SVM + XGBoost / RandomForest

🧩 可解释AI方向：用 SHAP / LIME 解释模型决策

🧠 研究方向：SVM 对不平衡分类、用户冷启动、推荐系统可扩展性分析